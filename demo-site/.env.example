# Base URL of the CORS proxy used by the recipe scraper. The target recipe
# URL is URL-encoded and appended. Falls back to the public worker if unset.
VITE_CORS_PROXY=https://cors.nicky.workers.dev/?target=
