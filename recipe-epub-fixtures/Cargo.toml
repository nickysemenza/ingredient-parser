[package]
name = "recipe-epub-fixtures"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT"
publish = false
description = "Deterministic in-memory EPUB cookbooks for downstream tests"

[dependencies]
zip = { version = "8", default-features = false }

[lints]
workspace = true
